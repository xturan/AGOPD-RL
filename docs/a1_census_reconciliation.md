# A.1 普查复核与修正方案(进行中,2026-09-08)

## 假设
A.1 的 **89.17%(16,164 题中 0/8=14,413)、mean p̂=0.0235** 极可能是**脏 prompt 造成的假象**,而非真实的学生几乎全错。
产出 A1 数字的 `p_hat_survey` 用 `PROMPT_TEMPLATE.format(problem=str(list-of-dicts))` 把**消息列表的 Python repr** 塞进模板当题目;该脏格式压低求解,故 89% 全失败。

## 一致性证据(已成立,无需跑即可推理)
- 干净 chat-template、同一模型:**val 固定评估 Base strict = 23.34%**(evaluate_dapo 协议);
- 干净 chat-template、train 匹配抽样 2,000 题×n=8:**mean p̂ = 0.229**(≈单次 23%),p0=49.5%。
- 两者一致 ⇒ 干净口径下单次成功 ~23%;A.1 的 mean p̂=0.0235(~2.4%)比它低 10× ⇒ **A.1 口径异常**。

## 决定性复现(进行中,云端)
- GPU0: survey 脏格式(复刻 `PROMPT_TEMPLATE.format(problem=str(...))`),120 题×n=8,1.7B;
- GPU1: 干净 chat-template,同 120 题×n=8。
- 预期:survey ≈ 0.8–0.9 全失败、chat ≈ 0.4–0.5(与 49.5% 匹配)。若成立 ⇒ 假设确认。

## 若确认,下游必须一起改(逐项核,不是只换一个数字)
1. **摘要 / §3.1 / 图1(a) / 表A1 / 附录 A.1**:89.17%、14,413、mean 0.0235 换成干净口径(re-run 或抽样 CI)。0/8 大概率 ~49%(train)而非 89%——"信号极稀疏/结果监督饥饿"叙事需降档重写(仍稀疏但非极端)。
2. **comfort 桶(1,472)与困难桶(3,120)构成**:原以 p̂ 阈值由脏普查划分;p̂̂≥0.25 只有 631 题(表A1 注记)若基于脏口径,舒适池定义可能整体偏移——需按干净口径重算桶构成,并说明对已训练实验的影响(训练本身不变,只影响"桶定义/动机"表述)。
3. **EGR/信号密度链条**:mean p̂=0.0235→~0.23 时,EGR(n6) 的"20%→80%"等叙述与 §2.3 的应用例子要重算。
4. **val 分桶(图2a/图4 雷达/表3 桶列)**:若这些桶由同一脏普查的 base p̂ 划分,需审计是否改用干净口径(§2.3/§3 的桶定义统一到一套 clean census)。
5. **"数据条件化是弱底座特需"等价值句**:改为只由 EGR(式2)与干净桶支撑,不再引用 89.17%。
> 注意:已训练的 GRPO/OPD/AGOPD 结果与四臂表不受影响(训练在 comfort 池上进行,模型已训);受影响的是**动机/普查/桶定义/叙事**这一类。

## 未决/需用户决策
- 复现确认后,是否重跑 1.7B 全量 train 普查(16,164×n=8,云端 ~1–2h)以给出权威新 A1 数字,还是用样本+CI(2,000/4,000)定档;
- §3.1/摘要等“信号极稀疏”改写幅度由你定(可能牵动引言与结论)。
- 4B 同口径(用于 A.3 匹配对比)待 A1 协议冻结后一并执行。


## 结案(2026-09-09 全量复现)
全量干净 chat 普查(16,164×n=8):**p0=48.9%[48.1,49.7]、mean p̂=0.232、S(6)=36.0%**;零命中 7,905。**A1 旧 89.17%/0.0235 确认作废**。已在 v6 全面传播:摘要(中/英)、§2.1、§3.1(+小节改名)、图1(a)、表A1、A.1/A.2/A.3、key-results 卡、叙事降档("高度稀疏/迅速下降"等删除);A.3 与 A1 同口径对齐(1.7B 全量 48.9% vs 4B 抽样 38.9%)。工件 census_17_full_summary.json;图1脚本计数已更新。

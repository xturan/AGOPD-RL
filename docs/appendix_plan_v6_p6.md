# P6 附录重排规划 v6.1(2026-09-09,先规划后动笔)

> 目标文件:reports/AGOPD_paper_v6_0_restructured_20260908.html。原则:附录与正文双轨;每步单独做、可校验;图按规范(图内无字/面板题放图下、统一配色、无网格);文字正式。

## 1. 总布局(改动后的目标)
| 块 | 内容 | 主要改动 |
|---|---|---|
| A | 普查与数据来源细则 | 保留 A.1(协议+表A1,48.9% 口径)、A.2(三类数据/1,472/3,120);**A.3 移出,单独成章** |
| B(新,原 A.3) | 底座规模与结果信号密度 | **独立章节**,讲 1.7B(48.9%)vs 4B(38.9% 抽样,待全量)+ S(6);与正文 A.3 引用点改指这里 |
| C(原 B) | 优化配置选择与冻结协议 | **内容/图表重规划**(见 §3) |
| D(原 C) | 训练动态健康检查与指标字典 | 图压缩、字典并入正文/留需项 |
| E(原 D) | 轨迹级迁移与多阶段谱系 | **删 图D1**;D2 按规范重画;D.1/D.2 文字重组(删 D1 引用) |
| F(原 E) | 输出预算与思考模式边界 | **大改**(见 §4) |
| G(原 F→重做) | 状态级反事实:轨迹与 token 分布 | **改名+重做**(见 §5,重点设计) |
| H(原 G) | 多次采样稳健性(Pass@k) | **图与表述重做**(见 §6) |
| Front | 正文—附录对照轨 | 最后统一加 |

> 字母只作顺序占位;正式字母待你定稿后一次性排。

## 2. A.3 单列
- 内容 = 现 A.3(1.7B 全量 48.9 / 4B 抽样 38.9 / S(6) 36.0→41.7 + 方向性结论 + 工件指针),升级为独立章节(标题如"底座规模与结果信号密度")。
- 正文凡引"A.3"改为引新章;补 4B 全量普查后再把 4B 抽样升级为全量并加 CI。

## 3. 优化配置(原 B)内容与图表重规划
- 目标:只回答"为什么主协议冻结为 全参数 lr1e-6 + KL0.05"。
- 内容:表 B1 保留并精简(rejected 行折叠/说明仅自证诚实),删 图B1/B2 或合并为**一张"更新强度 vs 稳定性"示意图**(示意可;或用 metrics_grpo_fullparam/lora 真数据画 "有效更新量+熵随 lr 通道" 一行)。采用哪种待定。
- 需先盘点 metrics_e1_full_lora / metrics_fp_collapse_lr1e5 / metrics_lora_lr3e4 等是否可支撑真图。

## 4. 输出预算与思考模式(原 E)大改
- 拆清三类:①纯解码预算探针(同检查点 2048→4096)②思考模式对照(测量对象=Stage-2C 等训练后模型,须写明"非 Base 普查代际")③继续 think-RL。
- 表重排、Δ 基线列显式;think 系数值不进摘要/不做主张;E.2 只留一行或删。

## 5. 状态级反事实重做(原 F,重点设计)
- 名字改:"状态级反事实:错误率前缀上的轨迹与 token 分布"一类(去掉"逐条/对照阅读"旧名)。
- 展示原则:不用滑块;用**颜色标注**:student 错误轨迹(红)、teacher 纠对续写(绿)、teacher 纠错续写(红字标注错因)、正确/错误前缀段的"原始正确轨迹"分别展示。
- 每个前缀档(Root/25%/50%/75%)给三行:原本正确轨迹、student 错误轨迹(红)、teacher 续写(纠正=绿 / 未纠正=红)。
- 新增 **token 层证据**:对每条 continuation 输出 token 的 top-16 概率分布,以**小型水平柱状图**逐 token 画;并按"teacher 与 student 判定的四种组合(都对 / teacher对-student错 / teacher错-student对 / 都错)"分桶,展示每桶的 **top-16 概率分布熵**的小柱图(或逐桶熵条形),说明"错对组合不同 → 分布熵/锐度不同"。
- 设计要点:**需重新采样**为每档×每题采集 teacher/student 的续写 + token 级 logprob/top-k 分布(现有工件只有结果正确率,缺 token 概率)→ 设计为附录诊断采样协议,先跑一个小样本验证再扩。
- 待你拍板:熵比较的横轴/分组口径;是否只在 2-3 个代表性题上做完整展示 + 全量统计在正文。

## 6. Pass@k(原 G)图与表述重做
- 表述:明确是"同预算多次采样覆盖"的稳健性背书,挂钩主张3"未引入多样性坍缩";删多余限定句。
- 图:按规范重画(横轴 k、五方法色一致、可加 k=1 参照);若需保留"难度偏易"说明放图注。

## 7. 执行顺序(每步改完校验一次)
1) A.3→独立章节 + 引用改指;
2) 优化配置 C 规划与图(先盘点数据再定画法);
3) 删 图D1 + 清理引用;D2 按规范重画嵌入;
4) 输出预算 E 大改;
5) 反事实 F 重做(先设计 token 采样协议 + 两个代表性题试点,再全量展示);
6) Pass@k 图/文;
7) Front 导读对照轨 + 全文字母/编号复核 + 打包。

> 每一步可能引入编号/引用漂移,统一在最后复核。
